# STATUS: borked, need to think some

format:
  name: arj
  mime: xxx
  parse_start:
    # XXX arj header is found by some routine that scans file backwards..., see arj.git
    - XXX code snippet to find first (last in file) arj header
  struct:
    - magic (ARJ archive)|60ea
    - XXX
