# STATUS: xxx

format:
  name: bmp
  mime: image/bmp
  details:
    - magic (BMP image)|byte:2|BM
    - file size|uint32le
    - reserved 1|uint16le
    - reserved 2|uint16le
    - offset to pixel data|uint32le
